# WITSML fixtures

Only synthetic or redistribution-cleared WITSML 2.0 projections may be stored here. Each fixture must retain UUID/URI identity, original unit symbols, provenance and a SHA-256 content hash.
