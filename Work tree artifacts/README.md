# Work tree artifacts

This directory keeps only small, human-readable notes that need to survive
worktree cleanup. Do not add compiled targets, executables, workbook outputs,
or JSONL logs: those are reproducible build artifacts and are ignored.
